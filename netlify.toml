# Netlify configuration file for Vite + React project

[build]
  command = "npm run build"
  publish = "dist"

# Optional: Redirect all routes to index.html for SPA routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
